# 2026-04-28 대시보드 수정 내역

## 1) 그래프 보기/정렬 UI 개선
- `가로 막대 / 세로 막대` 전환 기능 추가 및 동작 안정화
- `기본 순서 / 응답률 높은 순` 정렬 토글 추가
- 정렬 토글 적용 시 가로/세로 그래프 모두 동일한 기준으로 재배치되도록 수정

## 2) 세로 그래프 표시 개선
- 세로 막대에서 퍼센트(%) 표기 노출 방식 보완
- 막대 내부 수치 가독성 개선:
  - 글자색을 흰색으로 조정
  - 글자 크기를 1px 축소
- 세로 막대 간격/폭 조정으로 라벨 잘림 완화

## 3) 데이터 표 표시 제어
- `데이터 표 보기` 체크박스로 하단 표(응답보기/순위 표) 표시/숨김 제어 추가
- 체크박스 위치를 우측 컨트롤 영역으로 이동 및 정렬 보정

## 4) 범례/체크박스 영역 정렬 개선
- 1순위/2순위/3순위 체크박스 영역 정렬 정리 (좌측 정렬 중심)
- 전체 선택/전체 해제 버튼과의 배치 균형 수정

## 5) 그룹 설정 UX 개편
- 기존 우측 상시 그룹 편집 UI를 정리하고 `그룹 설정` 버튼 + 모달 방식으로 변경
- 모달에서 그룹 생성/편집/적용/닫기 흐름으로 동작하도록 개선
- 그룹 적용 시 우측 범례에 각 항목의 그룹 소속(색 인덱스 포함) 표시
- 그룹 적용 후 원복(원래대로 보기) 흐름 관련 동작 보완

## 6) 객관식 단일/중복/순위 시각화 정리
- 문항 타입별 그래프 표시 로직을 분리/보완해 잘못 겹쳐 보이던 케이스 수정
- 객관식 단일 문항에서 그룹 비교 적용 시 그래프가 회색 단색으로 깨지거나 수치가 사라지는 문제 대응

## 7) 그래프 유형 선택 방식 변경 (최신)
- 기존 버튼형 토글 대신 드롭다운으로 그래프 유형 선택 가능하도록 변경
- 선택 옵션:
  - `가로 막대`
  - `세로 막대`
  - `원형 그래프`
  - `누적 막대그래프`
- 관련 렌더링/이벤트 연결 추가:
  - 원형 그래프 렌더 함수 추가
  - 누적 막대 렌더 함수 추가
  - 드롭다운 변경 이벤트에 따라 즉시 재렌더링

## 8) 주요 수정 파일
- `C:\Users\FURSYS\Documents\GitHub\dashboard-for-survey\visualizations.js`
- `C:\Users\FURSYS\Documents\GitHub\dashboard-for-survey\dashboard.css`

## 9) 참고
- 오늘 작업 중 일부는 사용자 피드백에 맞춰 재조정(롤백/재적용)되었고,
  최종적으로는 현재 화면 기준 요구사항에 맞는 형태로 정리됨.
